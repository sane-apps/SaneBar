This folder is retained for a dormant experimental Setapp lane.

SaneBar's current public strategy is direct download only. Do not treat Setapp as an active distribution lane unless it is deliberately revived and re-validated.

If that happens later, place the Setapp-provided `setappPublicKey.pem` in this folder. The Setapp build script copies `/Users/sj/SaneApps/apps/SaneBar/Setapp/setappPublicKey.pem` into the app bundle at build time.
