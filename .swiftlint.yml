disabled_rules:
  - trailing_whitespace
  - todo
  - identifier_name
  - line_length
  - type_body_length
  - function_body_length
  - cyclomatic_complexity
  - nesting
  - shorthand_operator

file_length:
  warning: 1000
  error: 1200

opt_in_rules:
  - empty_count
  - empty_string
  - force_cast
  - force_try

force_cast: error
force_try: error

included:
  - Core
  - UI
  - SaneBarApp.swift

excluded:
  - Carthage
  - Pods
  - DerivedData
  - .build
  - .swiftpm
  - Tests
  - Scripts

# Configure specific rules if re-enabled later
